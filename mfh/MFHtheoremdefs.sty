% 
% Package for theorem definitions mfh typically uses
% Defines:
%   thm -> Theorem
%   lem -> Lemma
%   prop -> Proposition
%   cor  -> Corollary
%   defn -> Definition
%   exmp -> Example
%   rem  -> Remark
%   ex   -> Exercise
%   nte  -> Note
%
% Options:
%  book -> uses setup for a book
%  
%

\NeedsTeXFormat{LaTeX2e}

% update when new stuff added
\ProvidesPackage{MFHtheoremdefs}[2016/05/09]

%
% Required packages
%
\usepackage{amsthm}

%
% Parse options
%
\newif\if@book\@bookfalse
\DeclareOption{book}{
	\@booktrue
}

\ProcessOptions\relax

%
% The package
%

% By default amsthm uses a \topsep separation between its environments
% This sometimes a little small in my opinion. Especially if one (like I do)
% increases \parskip. This is why I add an extra \parskip spacing before
% the theorem enviroments.
\newtheoremstyle{plain}
	{\parskip\topsep} %space above
	{\topsep} %space below
	{\itshape} % body font
	{} % indent amount
	{\bfseries} % body head font
	{.} % punctuation after theorem head
	{.5em} % space after theory head
	{} % theorem head spec (empty is normal)

\newtheoremstyle{definition}
	{\parskip\topsep} %space above
	{\topsep} %space below
	{} % body font
	{} % indent amount
	{\bfseries} % body head font
	{.} % punctuation after theorem head
	{.5em} % space after theory head
	{} % theorem head spec (empty is normal)

\newtheoremstyle{note}
	{\parskip} % space above
	{} % space below
	{\normalfont} % body font
	{} % indent amount
	{\itshape} % body head font
	{.} % punctuation after theorem head
	{.5em} % space after theory head
	{} % theorem head spec (empty is normal)

% Define the theorems
\if@book
   % For a book: Theorem numbering is for each chapter
   \theoremstyle{plain}
   \newtheorem{thm}{Theorem} [chapter] % reset theorem numbering for each chapter
\else
   % Else: Theorem numbering is for each section
   \theoremstyle{plain}
   \newtheorem{thm}{Theorem} [section] % reset theorem numbering for each section
\fi

\newtheorem{lem}[thm]{Lemma}
\newtheorem{prop}[thm]{Proposition}
\newtheorem{cor}[thm]{Corollary}

\theoremstyle{definition}
\newtheorem{defn}[thm]{Definition}
\newtheorem{exmp}[thm]{Example}
\newtheorem{ex}[thm]{Exercise}
\newtheorem{rem}[thm]{Remark}

\theoremstyle{note}
\newtheorem*{nte}{Note}

\endinput
